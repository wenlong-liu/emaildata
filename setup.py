# -*- coding: utf-8 -*-

from distutils.core import setup

setup(
    name = 'emaildata',
    packages = ['emaildata'], # this must be the same as the name above
    version = '0.1',
    description = 'A Python package to automatically push notifications and send data.',
    author = 'Wenlong Liu',
    author_email = 'wliu14@ncsu.edu',
    url = 'https://github.com/wliu2016/emaildata', # use the URL to the github repo
    download_url = 'https://github.com/wliu2016/emaildata/archive/0.1.tar.gz', # I'll explain this in a second
    keywords = ['Python', 'email', 'notification', 'extract data'], # arbitrary keywords
    classifiers = [],
)